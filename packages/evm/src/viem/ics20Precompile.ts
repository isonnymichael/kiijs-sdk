import { Abi } from 'viem';

import { ICS20_PRECOMPILE_ABI } from '../precompiles';

/**
 * The Viem ABI for the ICS20 precompile contract.
 * @category Cosmos Interoperability
 */
export const VIEM_ICS20_PRECOMPILE_ABI = ICS20_PRECOMPILE_ABI as Abi;
